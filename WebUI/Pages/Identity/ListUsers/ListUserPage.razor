@page "/listUser"
@using WebUI.Components.DataGrid
@using WebUI.ServicesModel.Identity
@attribute [Authorize]

<PageTitle>Потребители</PageTitle>

@if (DataGrid is null)
{
    <div class="loader"></div>
}
else
{
    <DynamicDataGrid TData="ListUserModel" Data="@DataGrid"></DynamicDataGrid>
}